/** @type {import('next').NextConfig} */
const nextConfig = {
  // instrumentation.ts (src/instrumentation.ts) is auto-detected by Next.js 15+
  // No config needed — it runs on server startup automatically.
};

module.exports = nextConfig;


